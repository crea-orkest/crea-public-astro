#import '@content/Identifiable.fragment.graphql';

fragment VideoBlock on VideoRecord {
  ...Identifiable
  title
  media {
    height
    provider
    providerUid
    thumbnailUrl
    title
    url
    width
  }
  cloudinaryThumbnail
}
